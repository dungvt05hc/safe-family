/**
 * Thin convenience wrapper kept for backwards compatibility.
 * New code should use `useExternalLogin('Google')` directly.
 */
export { useExternalLogin as useGoogleLogin } from './useExternalLogin'

